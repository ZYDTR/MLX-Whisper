# Apple Silicon 本地语音处理流水线依赖

# 核心 MLX 框架
mlx>=0.10.0

# ASR - mlx-whisper (MLX 原生 Whisper)
mlx-whisper>=0.4.0

# UI - Gradio
gradio>=4.0.0

# ASR - lightning-whisper-mlx (可选)
# lightning-whisper-mlx>=0.0.10

# 声纹分割 - pyannote (替代 Senko)
# 注意: pyannote 需要 huggingface-hub<1.0
pyannote.audio>=3.1.0
torch>=2.0.0
torchaudio>=2.0.0
huggingface-hub>=0.26.0,<1.0  # pyannote 要求

# VAD - Silero VAD (通过 torch hub 加载)
# 已包含在 torch 中

# 音频处理
soundfile>=0.12.1
librosa>=0.10.1
numpy>=1.24.0

# ONNX 运行时 (可选，用于 Silero VAD ONNX 版本)
onnxruntime>=1.16.0

# 工具库
tqdm>=4.66.0
